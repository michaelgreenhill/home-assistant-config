---
# Rip out the sensors from configuration.yaml and put them into discrete config files

mqtt:
  #discovery: true
  discovery_prefix: homeassistant
  sensor: !include_dir_list ../entities/mqtt